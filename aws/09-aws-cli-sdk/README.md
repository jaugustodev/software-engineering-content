# AWS CLI, SDK, IAM Roles & Policies

## Hands-on para assistir

| Aula | Por que assistir |
|------|-----------------|
| AWS EC2 Instance Metadata - Hands On | Ver o `curl http://169.254.169.254/latest/meta-data/` na prática — útil para entender como apps dentro de EC2 obtêm credenciais |
| AWS CLI Profiles | Configurar múltiplos profiles (`--profile`) é essencial no dia a dia |
| AWS CLI with MFA | Ver como gerar session token com MFA via CLI |

## Teoria que posso explicar (pode pular as aulas)

- AWS EC2 Instance Metadata (conceito do IMDS)
- AWS SDK Overview
- Exponential Backoff & Service Limit Increase
- AWS Signature v4 Signing (Sigv4) — mecanismo interno, raramente implementado manualmente
